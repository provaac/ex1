{
  "tests": [
    {
      "name": "Tests ocults ex1",
      "setup": "python -m pip install --upgrade pip && if [ -f requirements.txt ]; then pip install -r requirements.txt; fi && pip install pytest && if [ -f .hidden-tests/requirements.txt ]; then pip install -r .hidden-tests/requirements.txt; fi",
      "run": "pytest -q .hidden-tests/tests/test_hidden.py",
      "input": "",
      "output": "",
      "comparison": "included",
      "timeout": 600,
      "points": 100
    }
  ]
}
